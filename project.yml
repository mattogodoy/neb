name: Neb
options:
  bundleIdPrefix: com.neb
  deploymentTarget:
    macOS: "26.0"
  xcodeVersion: "16.0"
  createIntermediateGroups: true

packages:
  NebCore:
    path: NebCore

targets:
  Neb:
    type: application
    platform: macOS
    sources:
      - Neb
    dependencies:
      - package: NebCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.neb.app
        PRODUCT_NAME: Neb
        MACOSX_DEPLOYMENT_TARGET: "26.0"
        OTHER_LDFLAGS: ["-Wl,-w"]
        SWIFT_VERSION: "5.10"
        CODE_SIGN_ENTITLEMENTS: Neb/Neb.entitlements
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.social-networking
    entitlements:
      path: Neb/Neb.entitlements
      properties:
        com.apple.security.app-sandbox: true
        com.apple.security.network.client: true

  NebTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - NebTests
    dependencies:
      - target: Neb
      - package: NebCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.neb.app.tests
        GENERATE_INFOPLIST_FILE: YES
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Neb.app/Contents/MacOS/Neb"
        BUNDLE_LOADER: "$(TEST_HOST)"
